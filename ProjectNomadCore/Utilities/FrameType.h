#pragma once

#include <cstdint>

namespace ProjectNomad {
    // Simple alias which allows both easy changing of frame type in one place as well as clarity as to what the type is
    //  intended to signify
    using FrameType = uint32_t;
}
